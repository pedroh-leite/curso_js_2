// Mais sobre arrays

let arr = [1, 4, 5, 6, 7];
let nomes = ["Pedro", "Henrique", "Leite"];
let bool = [true, false, true];

console.log(arr[1]); //Acessando o número 4 no array ARR
console.log(nomes[0]); //Acessando o "Pedro" no array NOMES
console.log(bool[2]); //Acessando o último true no array BOOL

console.log(arr[arr.length - 1]) //Acessando o último valor do array