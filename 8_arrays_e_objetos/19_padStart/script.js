// Metodos de STRING PADSTART

let num = "1";

// Adiciona um caractere varias vezes no inicio da string
// O primeiro valor é quantos caracteres a string deve ter e o segundo é o que será incluido
let milContra = num.padStart(4, "0");
console.log(milContra);