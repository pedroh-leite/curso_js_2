// Usando o metodo key de objetos

let carro = {
    portas: 2,
    portamalas: "200l",
    motor: "2.0",
}

// Exemplo: let carro = {portas(chave): 2(valor)}

console.log(carro);

// Como descobrir as chaves dos objetos
console.log(Object.keys(carro));


